version: "3.8"
services:
  raceedge:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DB_PATH=/data/raceedge.db
      - CORS_ORIGIN=*
    volumes:
      - raceedge-data:/data
    restart: unless-stopped

volumes:
  raceedge-data:
